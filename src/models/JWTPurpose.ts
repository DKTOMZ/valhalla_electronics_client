/**
 * Enum for options used when generating JWT 
 */
export enum JWTPurpose {
    // eslint-disable-next-line no-unused-vars
    EMAIL = 'email',
    // eslint-disable-next-line no-unused-vars
    RESET_PASSWORD = 'resetpassword'
}